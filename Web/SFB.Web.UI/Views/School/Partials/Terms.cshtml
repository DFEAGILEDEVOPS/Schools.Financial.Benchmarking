@using SFB.Web.Common
@model SFB.Web.UI.Models.SchoolViewModel

<div class="column-half chart-filter">
    <div class="form-group">
        <label class="form-label" for="Year">Year</label>
        <select name="Year" id="Year" class="form-control" onchange="DfE.Views.HistoricalCharts.RebuildCharts('school')">
            @foreach (var year in Model.Terms)
            {
                <option value="@year">@FormatFinancialYear(year, Model.EstablishmentType)</option>
            }
        </select>
    </div>
</div>

@helper FormatFinancialYear(string year, EstablishmentType schoolType)
{
    @( schoolType == EstablishmentType.Academies ? year : year.Replace('/', '-'))
}

