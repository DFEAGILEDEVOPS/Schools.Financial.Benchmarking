@using SFB.Web.UI.Helpers.Constants
@using SFB.Web.UI.Models
@model SFB.Web.UI.Models.SchoolListViewModel

<div id="result-list">
    <div aria-live="assertive" id="js-search-results-info">
        <div class="result-info">
            <p class="summary" aria-live="assertive">
                <span class="bold result-count">@(Model.Pagination.Total)</span> schools found
            </p>
        </div>
    </div>
</div>

@if (Model.ModelList.Count > 0)
{
    <ul>
        @foreach (var school in Model.ModelList)
        {
            <li class="document">
                <div class="grid-row">
                    <div class="column-half">
                        <a href="/school/detail?urn=@school.Id" class="bold-small">
                            @school.Name
                        @if (school.Status == "Closed")
                        {
                            <span> (Closed)</span>
                        }
                        </a>
                        <div class="addtocompare add-remove-mobile">
                            @AddRemoveButton(school)
                        </div>
                        <dl class="metadata">
                            <dd>@school.Address</dd>
                            <dd>@school.OverallPhase<span> (@school.TotalPupils pupils)</span></dd>
                            <dd>@school.Type</dd>
                            @if (!string.IsNullOrEmpty(school.OfstedRatingText))
                            {
                                <dd>Ofsted rating: <span class="rating rating-@school.OfstedRating"><span class="rating-text">@school.OfstedRating</span></span>@school.OfstedRatingText</dd>
                            }
                        </dl>
                    </div>
                    <div class="column-half addtocompare add-remove-desktop">
                        @AddRemoveButton(school)
                    </div>
                </div>
            </li>
        }
    </ul>

        @Html.Partial("Partials/Pagination", Model.Pagination)
}
else
{
    <p class="bold-small" style="margin-top: 22px;">Please try:</p>
        <p class="indent">
            • <a href="/">searching again</a> using different spelling or words
        </p>
        <p class="indent">
            • removing your filters
        </p>
}

@helper AddRemoveButton(SchoolViewModel school)
{
    <div id="@school.Id">
        <a @DisplayStyle(Model.SchoolComparisonList.BenchmarkSchools.Any(s => s.Urn == school.Id)) class='add-remove button removefrom' href="javascript:DfE.Views.SchoolsResultsViewModel.UpdateBenchmarkBasket(@school.Id,'removefromcompare')">Remove</a>
        <a @DisplayStyle(Model.SchoolComparisonList.BenchmarkSchools.All(s => s.Urn != school.Id)) class='add-remove button addto js-modal' data-urn="@school.Id" href="javascript:DfE.Views.SchoolsResultsViewModel.UpdateBenchmarkBasket(@school.Id,'addtocompare')">Add</a>
    </div>
}

@helper DisplayStyle(bool display)
{
if (display)
{
        @Html.Raw("style=\"display: inline-block\"");
    }
    else
    {
        @Html.Raw("style=\"display: none\"");
    }
}


